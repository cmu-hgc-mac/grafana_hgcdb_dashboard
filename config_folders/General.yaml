datasource_variable: DS_POSTGRES

dashboards:

  - title: "Enviorment Monitoring"
    panels:
      - title: "Temperature (°C)"
        table: "temp_humidity"
        chart_type: "timeseries"
        condition: "temp_humidity.temp_c IS NOT NULL"
        groupby: ["log_timestamp", "temp_c"]
        filters: ["log_location"]
        filters_table: "temp_humidity"
        distinct: False
      - title: "Real Humidity"
        table: "temp_humidity"
        chart_type: "timeseries"
        condition: "temp_humidity.rel_hum IS NOT NULL"
        groupby: ["log_timestamp", "rel_hum"]
        filters: ["log_location"]
        filters_table: "temp_humidity"
        distinct: False