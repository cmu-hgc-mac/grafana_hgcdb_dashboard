# Setting for Grafana

GF_URL: "http://localhost:3000" # default
    # If there's a vpn connected, try http://127.0.0.1:3000
GF_SA_NAME: 
GF_SA_ID: 
GF_DATA_SOURCE_NAME: 
GF_API_KEY: 

GF_USER: 'admin' # default
GF_PASS: 'admin' # default

GF_defaults_PATH: '/Users/chensiyu/grafana-v12.0.0/conf/defaults.ini' 
    # by default, 'defaults.ini' should be in the conf directory of Grafana