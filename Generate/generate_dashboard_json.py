
logic for this script:
read everything in dashboard_json folder:
if xxx exist:
    with read mode = 'r', 
        修改
    with read mode = 'w',
        写入

else:
    create new json file,
    saved in dashboard_json folder